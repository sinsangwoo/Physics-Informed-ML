# AWS Lambda deployment requirements
# Minimal dependencies for serverless deployment

# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
mangum>=0.17.0  # ASGI adapter for AWS Lambda

# ML dependencies (keep minimal for cold start)
torch>=2.1.0
numpy>=1.24.0

# API dependencies
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Utilities
loguru>=0.7.0

# Optional: Remove if bundle too large
# scikit-learn>=1.3.0
# matplotlib>=3.7.0
